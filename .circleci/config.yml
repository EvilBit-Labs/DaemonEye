# This config was automatically generated from your source code
# Stacks detected: cicd:github-actions:.github/workflows,deps:rust:.
version: 2.1
jobs:
  test-rust:
    docker:
      - image: cimg/rust:1.85
    steps:
      - checkout
      - restore_cache:
          key: cargo-{{ checksum "Cargo.lock" }}
      - run:
          command: cargo install cargo-binstall
      - run:
          command: cargo binstall cargo-nextest --locked
      - run:
          command: cargo binstall just
      - run:
          command: cargo nextest run --workspace --exclude sentinel
      - save_cache:
          key: cargo-{{ checksum "Cargo.lock" }}
          paths:
            - ~/.cargo
  deploy:
    # This is an example deploy job, not actually used by the workflow
    docker:
      - image: cimg/base:stable
    steps:
      # Replace this with steps to deploy to users
      - run:
          name: deploy
          command: "#e.g. ./deploy.sh"
      - run:
          name: found github actions config
          command: ":"
workflows:
  build-and-test:
    jobs:
      - test-rust
    # - deploy:
    #     requires:
    #       - test-rust
