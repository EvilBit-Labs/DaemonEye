[package]
name = "unix-named-pipe"
version = "0.2.0"
authors = ["Windows Compatibility Patch"]
license = "MIT"
description = "Windows-compatible stub for unix-named-pipe"

[dependencies]
errno = "0.2.8"
libc = "0.2"
